# Security Toolkit 2.0

Безопасный генератор паролей и passphrase с улучшенной архитектурой и контейнеризацией.

## 🛡 Особенности безопасности

### Криптографическая безопасность
- **Безопасная генерация случайных чисел**: Используется `crypto.getRandomValues()` с rejection sampling для устранения модульного смещения (modulo bias)
- **BIP39 словарь**: Используется стандартный словарь из 2048 слов для генерации passphrase с высокой энтропией
- **Клиентская генерация**: Все пароли генерируются локально в браузере, никакие данные не передаются на сервер

### Архитектурные улучшения
- **Модульность**: Код организован в модули (IIFE pattern)
- **Явный доступ к DOM**: Нет неявных глобальных переменных
- **Разделение ответственности**: Логика генерации отделена от работы с DOM
- **Валидация входных данных**: Проверка всех пользовательских входных данных
- **Обработка ошибок**: Корректная обработка всех возможных ошибок

### Инфраструктурная безопасность
- **Security Headers**: Все необходимые security headers настроены в Nginx
- **Content Security Policy**: Строгая CSP политика
- **Docker security**: Минимальный образ, ограничение ресурсов, no-new-privileges
- **Nginx hardening**: Отключение server tokens, оптимизация безопасности

## 📋 Требования

- Docker 20.10+
- Docker Compose 2.0+

## 🚀 Быстрый старт

### Сборка и запуск

```bash
# Клонировать репозиторий (если нужно)
cd password_gen

# Собрать и запустить контейнер
docker-compose up -d --build

# Приложение будет доступно на http://localhost:8888
```

### Проверка работоспособности

```bash
# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f

# Проверить healthcheck
curl http://localhost:8888/health
```

### Остановка

```bash
docker-compose down
```

## 📁 Структура проекта

```
password_gen/
├── www/                  # Статические файлы
│   ├── index.html       # Главная страница
│   ├── style.css        # Стили
│   ├── app.js           # Основная логика приложения
│   └── bip39-words.js   # BIP39 словарь (2048 слов)
├── nginx/               # Конфигурация Nginx
│   ├── nginx.conf       # Основная конфигурация
│   └── default.conf     # Конфигурация виртуального хоста
├── Dockerfile           # Образ Docker
├── docker-compose.yml   # Docker Compose конфигурация
├── .dockerignore        # Исключения для Docker
└── README.md           # Документация
```

## 🔒 Детали безопасности

### Security Headers

Приложение настроено с следующими security headers:

- **X-Frame-Options: DENY** - защита от clickjacking
- **X-Content-Type-Options: nosniff** - предотвращение MIME-sniffing
- **X-XSS-Protection: 1; mode=block** - защита от XSS
- **Referrer-Policy: strict-origin-when-cross-origin** - контроль referrer
- **Permissions-Policy** - ограничение доступа к API браузера
- **Content-Security-Policy** - строгая CSP политика

### Генерация случайных чисел

Используется алгоритм rejection sampling для устранения модульного смещения:

```javascript
function secureRandom(max) {
  const maxValid = Math.floor(2**32 / max) * max;
  const arr = new Uint32Array(1);
  let value;
  do {
    crypto.getRandomValues(arr);
    value = arr[0];
  } while (value >= maxValid);
  return value % max;
}
```

### Энтропия passphrase

- **BIP39 словарь**: 2048 слов = ~11 бит энтропии на слово
- **Рекомендуется**: 6 слов = ~66 бит энтропии (достаточно для большинства случаев)
- **Максимум**: 12 слов = ~132 бит энтропии (очень высокая безопасность)

### Проверка силы пароля

Алгоритм учитывает:
- Длину пароля
- Разнообразие символов (верхний/нижний регистр, цифры, символы)
- Штраф за повторяющиеся символы

## 🔧 Конфигурация

### Изменение порта

Отредактируйте `docker-compose.yml`:

```yaml
ports:
  - "8888:80"  # Измените 8888 на нужный порт
```

### Настройка Nginx

Основные настройки находятся в:
- `nginx/nginx.conf` - основная конфигурация
- `nginx/default.conf` - конфигурация виртуального хоста

### Использование HTTPS (рекомендуется для продакшена)

1. Получите SSL сертификаты (например, через Let's Encrypt)
2. Добавьте конфигурацию SSL в `nginx/default.conf`:

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    # SSL настройки
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    
    # HSTS
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    
    # ... остальная конфигурация
}
```

3. Добавьте редирект с HTTP на HTTPS

## 🚀 Деплой на продакшн

Подробное руководство по деплою на продакшн сервер находится в файле [DEPLOY.md](DEPLOY.md).

### Быстрый деплой

```bash
# Использовать скрипт деплоя
./deploy.sh

# Или вручную
docker compose up -d --build
```

Основные шаги:
1. Подготовить сервер (Docker, Docker Compose)
2. Скопировать проект на сервер
3. Настроить SSL сертификаты (Let's Encrypt)
4. Обновить конфигурацию Nginx для HTTPS
5. Запустить контейнер

**Важно для продакшна:**
- Использовать HTTPS (SSL сертификаты)
- Настроить файрвол
- Включить мониторинг
- Настроить автообновление SSL сертификатов

См. [DEPLOY.md](DEPLOY.md) для детальных инструкций.

## 📊 Производительность

- **Размер образа**: ~25 MB (nginx:alpine)
- **Потребление памяти**: ~20-30 MB
- **Время запуска**: < 1 секунды
- **Время ответа**: < 10 мс

## 🧪 Тестирование

### Локальное тестирование

Откройте `www/index.html` в браузере (требуется локальный веб-сервер из-за CSP):

```bash
# Python
cd www && python3 -m http.server 8000

# Node.js
cd www && npx http-server
```

### Тестирование контейнера

```bash
# Проверка здоровья
curl http://localhost:8888/health

# Проверка основного приложения
curl http://localhost:8888/

# Проверка security headers
curl -I http://localhost:8888/
```

## 🐛 Устранение неполадок

### Контейнер не запускается

```bash
# Проверить логи
docker-compose logs web

# Проверить конфигурацию Nginx
docker-compose exec web nginx -t
```

### Приложение не открывается

- Убедитесь, что порт 8888 не занят: `lsof -i :8888`
- Проверьте, что контейнер запущен: `docker-compose ps`
- Проверьте логи: `docker-compose logs -f web`

### Проблемы с CSP

Если видите ошибки CSP в консоли браузера, проверьте конфигурацию в `nginx/default.conf`.

## 📝 Лицензия

Проект предоставляется "как есть" для использования.

## 🙏 Благодарности

- BIP39 word list: https://github.com/bitcoin/bips/blob/master/bip-0039/english.txt
- Nginx: https://nginx.org/
- Docker: https://www.docker.com/

## 🔗 Полезные ссылки

- [OWASP Security Headers](https://owasp.org/www-project-secure-headers/)
- [Content Security Policy](https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP)
- [BIP39 Standard](https://github.com/bitcoin/bips/blob/master/bip-0039.mediawiki)
- [Web Crypto API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Crypto_API)
